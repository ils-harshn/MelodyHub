import { styled } from "styled-components";

export const Container = styled.div`
    /* width: 100%; */
    max-width: 1140px; /* Adjust the value as per your preference */
    margin: 0 auto 0 auto;
`